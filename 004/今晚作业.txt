1、通过定时器2中断，实现200ms中断变更LED灯状态。


2、通过定时器1中断，实现1000ms中断变更LED灯状态。
更新中断通道：TIM1_UP_TIM10_IRQn

3、通过定时器3的1ms中断，分别实现下面LED0效果。

a.亮1ms,灭1ms。
b.亮1ms,灭2ms。
c.亮1ms,灭4ms。
d.亮1ms,灭8ms。

4、复习今天课程内容、预习明天课程。